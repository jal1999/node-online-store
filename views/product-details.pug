extends layouts/main-layout.pug
block styles
    link(rel='stylesheet' href='/css/store.css')
block content
    .product#details
        h1 #{product.title}
        img#detail(src=product.imageUrl)
        h3 $#{product.price}
        h2 #{product.description}
        .buttons#back
            button
                a(href='/') Back to store